{
  "name": "daily-tasks-app",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla HTML/CSS/JS personal to-do list app",
  "scripts": {
    "start": "serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.6"
  }
}
